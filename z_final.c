#include<stdio.h>


int find_coeff()
{
	
}

int main()
{
	int test_cases;
	scanf("%d",&test_cases);

	int *term = (int*)malloc(sizeof(int))
	int coeff;

	for(i=0;i<test_cases;i++)
	{
		scanf("%d%d%d",&a,&b,&n);

		if(n%2 == 0)
			coeff = find_coeff(n);
}